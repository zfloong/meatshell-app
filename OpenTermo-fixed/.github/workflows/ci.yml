name: CI

on:
  push:
    branches: [zfl]
    tags: ['Vzfl*']
  pull_request:
    branches: [zfl]

# Cancel in-progress runs when a new push arrives
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: build & check (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      # ---- Node.js ----
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('package-lock.json') }}
          restore-keys: ${{ runner.os }}-npm-

      - name: Install frontend dependencies
        run: npm install

      # ---- Generate Tauri icons (from custom icon.png) ----
      - name: Generate Tauri icon set
        run: npx tauri icon src-tauri/icons/icon.png

      # ---- Frontend build ----
      - name: TypeScript check + Vite build
        run: npm run build

      # ---- Rust backend (meatshell + Tauri) ----
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-v2-${{ hashFiles('src-tauri/Cargo.toml', 'meatshell/Cargo.toml') }}
          restore-keys: ${{ runner.os }}-cargo-v2-

      - name: cargo check (Tauri + meatshell, with retry)
        shell: pwsh
        run: |
          $attempt = 0
          while ($true) {
            try {
              cargo check 2>&1 | Out-Host
              if ($LASTEXITCODE -eq 0) { break }
              throw "cargo check exit code: $LASTEXITCODE"
            } catch {
              $attempt++
              if ($attempt -ge 3) { throw }
              Write-Host "Retrying cargo check (attempt $attempt/3) in 15s..."
              Start-Sleep -Seconds 15
            }
          }
        working-directory: src-tauri

      # ---- Full Tauri build ----
      - name: Tauri build (with retry)
        shell: pwsh
        run: |
          $attempt = 0
          while ($true) {
            try {
              npx tauri build 2>&1 | Out-Host
              if ($LASTEXITCODE -eq 0) { break }
              throw "tauri build exit code: $LASTEXITCODE"
            } catch {
              $attempt++
              if ($attempt -ge 3) { throw }
              Write-Host "Retrying tauri build (attempt $attempt/3) in 15s..."
              Start-Sleep -Seconds 15
            }
          }
        env:
          TAURI_SKIP_DEVSERVER_CHECK: "true"

      # ---- Show what was built ----
      - name: List build outputs
        shell: pwsh
        run: |
          Write-Host "=== Bundle dir contents ==="
          if (Test-Path src-tauri/target/release/bundle/) {
            Get-ChildItem -Recurse src-tauri/target/release/bundle/ -ErrorAction SilentlyContinue | ForEach-Object { Write-Host $_.FullName }
          } else {
            Write-Host "(no bundle directory)"
          }
          Write-Host "=== Exe ==="
          Get-ChildItem src-tauri/target/release/*.exe -ErrorAction SilentlyContinue | ForEach-Object { Write-Host $_.FullName }
          Write-Host "=== Target release root ==="
          Get-ChildItem src-tauri/target/release/ -ErrorAction SilentlyContinue | ForEach-Object { Write-Host $_.FullName }

      # ---- GitHub Release (tag-triggered) ----
      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/Vzfl')
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe

      # ---- Upload build artifacts ----
      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: opentermo-win64
          path: |
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: warn
          retention-days: 7
